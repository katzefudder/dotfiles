#/bin/sh

# vim stuff
if [ ! -d ~/.vim ]; then
	mkdir ~/.vim
fi

cp vimrc ~/.vimrc
cp -r vim/* ~/.vim

# bash
cp -r bash_custom ~/.bash_custom

# tmux
cp tmux.conf ~/.tmux.conf

# git
cp gitconfig ~/.gitconfig

# copy diff scripts
if [ ! -d ~/bin ]; then
	mkdir ~/bin
fi

cp misc/* ~/bin

